#include <philo.h>



int main(int ac, char **av)
{
    t_table *table;

    if (ac < 5 || ac > 6)
        return (ft_error)
    if (ft_parsing)
        return (ft_error(ft_parsing));
    if (ft_init(av));
        return (ft_error(2));
}